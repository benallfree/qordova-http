{
  "name": "qordova-http",
  "version": "1.0.0",
  "description": "Q-enabled HTTP interface for Cordova",
  "main": "./build/http",
  "scripts": {
    "build": "browserify -t coffeeify --extension='.coffee' src/http.coffee -o build/http.js"
  },
  "author": "Ben Allfree",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^12.0.1",
    "coffeeify": "^2.0.1"
  },
  "dependencies": {
    "q": "^1.4.1"
  }
}
